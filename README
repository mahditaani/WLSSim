# This is a program to simulate the propagation of a photon in a wavelength shifter

# USAGE
root -l main.cpp # To run the simulation
root -l SeePlots.C # To see the heatmaps

# Variables
numPhots	Number of photons to simulate
numBounce	Maximum number of reflections to simulate
PMTRadius	Radius of the PMT
WLSLength	Full length of the WLS edge

